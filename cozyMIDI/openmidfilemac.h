#ifndef __OPENMIDIFILEMAC__#define __OPENMIDIFILEMAC__short openMIDIFile(void);extern OSErr gOSErr;extern Boolean gVerbo;extern Handle gMidiBuf;int midiDialogLoadAndPrep(void); //entry function for one stop shop in a midi player type environmentint getAndAnalyzeMIDI(void); //high level function that directs the reading and parsing of the MIDI file    short openMIDIFile(void); //uses std file dialog to pick a MID or MIDI signature file and return a short file numberint findPositionOfHeader(void); //this finds the start position of the 'MThd' stringextern OSErr fOSErr; //deal with error codes in your main source files however you wantextern Handle gMidiBuf; // Handle to the raw midi buffer data read from a file#endif __OPENMIDIFILEMAC__