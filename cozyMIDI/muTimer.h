#ifndef MUTIMER_H#define MUTIMER_H#include "Timer.h"typedef struct{TMTask atmTask;long tmRefCon; //space to pass the address of A5 world} TMInfo, *TMInfoPtr;pascal void myTask(void);void installTMTask(void);  //installs the Time Manager task, used to get microsecond level interrupts with the revised Time Manager of System 6.0.3+void installTMAnim(void); //installs TM anim task in msvoid installTMSpeaker(void);extern TMInfo myTMInfo, animTMInfo, speakerTMInfo;extern long gDelay;extern Boolean readyForNextNote; //makes it advance the next midi notesextern Boolean greenAnim; //makes it advance the gfx animation framesextern Boolean greenSpeaker;#endif