#ifndef MUTIMER_C#define MUTIMER_C#include "muTimer.h"#include "Timer.h"Boolean readyForNextNote = true;Boolean greenAnim = false;Boolean greenSpeaker = false;pascal TMInfoPtr GetTMInfo(void)    = 0x2E89;   //MOVE.L A1, (SP)    TMInfo  myTMInfo, animTMInfo, speakerTMInfo;//the interrupt task just sets a flag to indicate it's ready to receive the pending MIDI event in the parsed structure.pascal void myTask()	{	long oldA5;	TMInfoPtr recPtr;		recPtr = GetTMInfo();	oldA5 = SetA5(recPtr->tmRefCon);		readyForNextNote = true;	oldA5 = SetA5(oldA5);	}	pascal void animTask()	{	long oldA5;	TMInfoPtr recPtr;		recPtr = GetTMInfo();	oldA5 = SetA5(recPtr->tmRefCon);		greenAnim = true;	oldA5 = SetA5(oldA5);	}pascal void speakerTask()	{	long oldA5;	TMInfoPtr recPtr;		recPtr = GetTMInfo();	oldA5 = SetA5(recPtr->tmRefCon);		greenSpeaker = true;	oldA5 = SetA5(oldA5);	}	//installs the Time Manager task, used to get microsecond level interrupts with the revised Time Manager of System 6.0.3+void installTMTask(void)	{	myTMInfo.atmTask.tmAddr = myTask;	myTMInfo.atmTask.tmWakeUp = 0;	myTMInfo.atmTask.tmReserved = 0;	myTMInfo.tmRefCon = SetCurrentA5();	InsTime((QElemPtr) &myTMInfo);	}void installTMAnim(void)	{	animTMInfo.atmTask.tmAddr = animTask;	animTMInfo.atmTask.tmWakeUp = 0;	animTMInfo.atmTask.tmReserved = 0;	animTMInfo.tmRefCon = SetCurrentA5();	InsTime((QElemPtr) &animTMInfo);	}	void installTMSpeaker(void)	{	speakerTMInfo.atmTask.tmAddr = speakerTask;	speakerTMInfo.atmTask.tmWakeUp = 0;	speakerTMInfo.atmTask.tmReserved = 0;	speakerTMInfo.tmRefCon = SetCurrentA5();	InsTime((QElemPtr) &speakerTMInfo);	}#endif